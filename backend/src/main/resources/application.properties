spring.application.name=backend

spring.datasource.url=jdbc:mysql://localhost:3306/expiry_tracker
spring.datasource.username=root
spring.datasource.password=Mysql@2005

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
google.vision.api.key=${GOOGLE_VISION_API_KEY:}

server.port=8081

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:expirylens@gmail.com}
spring.mail.password=${MAIL_PASSWORD:Expirylens@2005}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:true}
app.mail.from=${MAIL_FROM:expirylens@gmail.com}
app.mail.from-name=${MAIL_FROM_NAME:ExpiryLens Notifications}
app.brand.name=${APP_BRAND_NAME:ExpiryLens}

expiry.reminder.cron=${EXPIRY_REMINDER_CRON:0 0 * * * *}